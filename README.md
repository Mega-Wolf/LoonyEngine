# LoonyEngine

This is a little demo project showing the differences between a normal N x N collision check, a quadtree and a simple grid structure.
One can choose which methods can be compared directly in the scene on the SuperPhysicsManager script.